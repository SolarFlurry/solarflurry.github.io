(function($) {
  //plugin code
  $.newSprite = function(custom) {
    var defs = {image: "placeholder.png", x:0, y:0, width:50, height:50},
    var ops = $.extend(defs, custom),
    var gameScreen = $("#gamescreen")
  }
}(jQuery)
